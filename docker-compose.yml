version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      DIRECT_URL: ${DIRECT_URL}
    env_file:
      - .env
    # NO postgres service needed anymore!

# Remove the pgdata volume too